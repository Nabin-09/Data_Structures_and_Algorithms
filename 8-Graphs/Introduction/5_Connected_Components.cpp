#include <bits/stdc++.h>
using namespace std;

int main() {
    /*Connected components if graphs are distributed into portions
    it can still be called a single graph even if all the vertices are not connected.
    we can use a visited array to check what verties have we visited.
    mark all the nodes as 0 , the 
    for(0 -> n){
        if(!visited){
            traversal(i);
            //This will cause all the connected portion to be marked visted.
            same happens for all distributed portions(n).        
        }
    }*/   
    return 0;
}