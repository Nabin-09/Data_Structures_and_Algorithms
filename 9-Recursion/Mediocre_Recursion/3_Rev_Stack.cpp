
#include <bits/stdc++.h>

using namespace std;

int main()
{
    // We need to reverse a stack
    std::vector<int> values = {1, 2, 3, 4}; // Create a stack and push the values from the vector
    std::stack<int> s;
    for (int value : values)
    {
        s.push(value);
    }
}